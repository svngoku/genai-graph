# ReAct Agent Demos Configuration
langchain_agents:
  defaults:
    type: react
    middlewares:
      - class: genai_tk.agents.langchain.middleware.rich_middleware:RichToolCallMiddleware
    checkpointer:
      type: none

  default_profile: "EKG"

  profiles:
    - name: "EKG"
      description: "EKG-aware ReAct agent that queries the knowledge graph via Cypher"
